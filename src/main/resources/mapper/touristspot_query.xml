<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.enjoytrip.touristspot.dao.TouristspotDao">

    <select id="findAll" parameterType="com.example.enjoytrip.common.dto.PageDto" resultType="com.example.enjoytrip.touristspot.domain.TouristSpot">
        select *
        from tourist_spot, review
        where tourist_spot.touristspot_id = review.touristspot_id
        limit #{pageSize} offset #{pageNum}
    </select>

    <select id="findBySido" parameterType="map" resultType="com.example.enjoytrip.touristspot.domain.TouristSpot">
        select *
        from tourist_spot
        where touristspot_sido_code = #{sidoCode}
        limit #{pageDto.pageSize} offset #{pageDto.pageNum}
    </select>

    <select id="findByGugun" parameterType="map" resultType="com.example.enjoytrip.touristspot.domain.TouristSpot">
        select *
        from tourist_spot
        where touristspot_gugun_code = #{gugunCode}
        limit #{pageDto.pageSize} offset #{pageDto.pageNum}
    </select>

    <select id="findByKeyword" parameterType="map" resultType="com.example.enjoytrip.touristspot.domain.TouristSpot">
        select *
        from tourist_spot
        where touristspot_title like concat('%', #{keyword}, '%')
        limit #{pageDto.pageSize} offset #{pageDto.pageNum}
    </select>

    <select id="findById" parameterType="int" resultType="com.example.enjoytrip.touristspot.domain.TouristSpot">
        select *
        from tourist_spot
        where touristspot_id = #{touristspotId}
    </select>

    <select id="findByCoordinates" parameterType="map" resultType="com.example.enjoytrip.touristspot.domain.TouristSpot">
        SELECT *
        FROM tourist_spot
        WHERE touristspot_latitude &lt;= #{touristCoordinateDto.northLatitude} AND touristspot_latitude &gt;= #{touristCoordinateDto.southLatitude}
          AND touristspot_longitude &lt;= #{touristCoordinateDto.eastLongitude} AND touristspot_longitude &gt;= #{touristCoordinateDto.westLongitude}
            LIMIT #{pageDto.pageSize} OFFSET #{pageDto.pageNum}
    </select>

    <select id="relatedBoard" parameterType="map" resultType="com.example.enjoytrip.board.dto.BoardDto">
        SELECT *
        FROM board
        WHERE touristspot_id = #{touristSpotId}
    </select>
    <select id="relatedTitle" parameterType="String" resultType="com.example.enjoytrip.touristspot.domain.TouristSpot">
        SELECT touristspot_id, touristspot_title
        FROM tourist_spot
        <if test='touristspotTitle!=null'>where touristspot_title like concat('%', #{touristspotTitle}, '%')</if>
        order by touristspot_title
    </select>

    <delete id = "touristspotDelete" parameterType="Integer">
        delete from board where board_id = #{touristSpotId}
    </delete>

    <select id = "touristspotRecommendList" parameterType="Integer">
        select touristpot_id from like_spot where account_id = #{accountId} order by touristpot_id desc
    </select>

    <select id = "touristspotRecommendCount" parameterType="Integer">
        select count(account_id) from like_spot where touristpot_id = #{touristSpotId}
    </select>

    <insert id = "touristspotRecommendInsert">
        insert into like_spot (touristpot_id, account_id)
        values (#{touristspotId}, #{accountId})
    </insert>

    <delete id = "touristspotRecommendDelete" parameterType="Integer">
        delete from like_spot where touristpot_id = #{touristspotId} and account_id = #{accountId}
    </delete>



</mapper>
